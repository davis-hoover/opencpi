<ComponentSpec>
  <Property Name="DAC_WIDTH_BITS"
            Type="UChar"
            Parameter="True"
            Default="16"
            Description="Number of bits in each of the I and Q values delivered to the DAC device"/>
  <Property Name="DAC_OUTPUT_IS_LSB_OF_IN_PORT" 
            Type="Bool" 
            Parameter="True"
	    Default="false"
            Description="Indicates whether DAC data is extracted from the most
                         significant or least significant DAC_WIDTH_BITS of the in port."/>
  <Property Name="GP_CTRL_ARRAY_LENGTH"
            Type="Ushort"
            Parameter="true"
            Default="1"/>
  <Property Name="GP_STATUS_ARRAY_LENGTH"
            Type="Ushort"
            Parameter="true"
            Default="1"/>
  <Property Name="underrun_sticky_error"
            Type="Bool"
            Volatile="true"
            Description="Sticky bit indication of samples underrun."/>
  <Property Name="clr_underrun_sticky_error" 
            Type="Bool"
            Writable="true"
            Description="Writing true clears underrun_sticky_error."/>
  <Property Name="gp_ctrl"
            Type="Ulonglong"
            ArrayLength="GP_CTRL_ARRAY_LENGTH"
            Writable="true"
            Description="General purpose control. Value of all zeros results in
                         no action."/>
  <Property Name="gp_status"
            Type="Ulonglong"
            ArrayLength="GP_STATUS_ARRAY_LENGTH"
            Volatile="true"
            Description="General purpose status."/>

  <Port Name="in" 
        Protocol="ComplexShortWithMetadata-prot"
        Optional="1"/>
  <Port Name="on_off" 
        Protocol="tx_event-prot"
        Optional="1"
        Producer="true"/>
</ComponentSpec>
